import announceToScreenReader from "@styles/announce-to-screen-reader";

const announceHighlight = announceToScreenReader(
  "highlight start",
  "highlight end"
);

export default announceHighlight;
