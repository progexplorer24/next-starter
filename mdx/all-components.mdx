import MainContainer from "../layouts/main-container";
import Code from "../components/code/code";

<MainContainer>

<Code>
  <pre className="language-tsx">{props.examples.join("\n")}</pre>
</Code>

**Headings Example:**

# Heading 1

## Heading 2

### Heading 3

#### Heading 4

##### Heading 5

###### Heading 6

**Image Example:**

![Computer generated bacteria image](../imgs/bacteria.jpg)

**Paragraph & inline code & link example:**

To _control_ the font size of an ~~element~~ at a specific breakpoint, add a `{screen}:` prefix to any existing font size utility. For example, use `md:text-lg` to apply the `text-lg` utility at only medium screen sizes and above.

For more information about Tailwind's responsive design features, check out the [Responsive Design](https://tailwindcss.com/docs/responsive-design) documentation.

**Table Example:**

| Tables | Are  | Cool |
| ------ | ---- | ---- |
| El.1   | El.2 | El.3 |

**List Example:**

---

- Headline 1
- Headline 2
- Subtitle 1
- Caption
- List Element

---

1. Headline 1
2. Headline 2
3. Subtitle 1
4. Caption
5. List Element

> Black Lives Matter

```js
import React, { ReactNode } from "react";
import { css } from "@emotion/core";
import tw from "tailwind.macro";

// Comments
<input
  type="text"
  placeholder="Search by keywords"
  css={css`
    ${tw`w-full py-2 pl-10 pr-4 mr-4 text-white bg-gray-900 rounded-lg focus:outline-none focus:bg-gray-100 focus:text-gray-900`}
    &:focus {
      &::placeholder {
        ${tw`text-gray-800`}
      }
    }
    &::placeholder {
      ${tw`text-gray-400`}
    }
  `}
/>;
```

```bash
npm install --save gatsby-plugin-emotion @emotion/core @emotion/styled
```

```tsx {1-3,22}
import React, { ReactNode } from "react"
import { css } from "@emotion/core"
import tw from "tailwind.macro"

const x = 5
Object.entries()

import { ReactComponent as Logo } from "../images/logo.svg"
import { ReactComponent as HamburgerIcon } from "../images/icon-menu.svg"
import { ReactComponent as SearchIcon } from "../images/icon-search.svg"
import { ReactComponent as FilterIcon } from "../images/filter-icon.svg"
import { ReactComponent as IconStar } from "../images/icon-star.svg"
import Villa from "../images/villa.jpg"

type WorkcationProps = {
  children: ReactNode
}

const Workcation: React.FC<WorkcationProps> = () => {
  return (
    <div
      css={css`
        ${tw`min-h-screen bg-gray-200`}
      `}
    >
      <header
        css={css`
          ${tw`flex items-center justify-between px-4 py-4 bg-gray-900`}
        `}
      >
        <Logo />

        <button
          css={css`
            ${tw`inline-flex items-center justify-center w-10 h-10 focus:outline-none focus:shadow-outline`}
          `}
        >
          <HamburgerIcon
            css={css`
              ${tw`w-6 h-6 text-gray-500 fill-current`}
            `}
          />
        </button>
      </header>
      <section
        css={css`
          ${tw`flex items-center justify-between px-4 py-4 bg-gray-800`}
        `}
      >
        <div
          css={css`
            ${tw`relative flex items-center`}
          `}
        >
        </div>
      </main>
    </div>
  )
}

export default Workcation
```

```jsx
// Dump component
const Dump = (props) => (
  <div
    style={{
      fontSize: 20,
      border: "1px solid #efefef",
      padding: 10,
    }}
    css={css`
      background: "red";
    `}
  >
    {Object.entries(props).map(([key, val]) => (
      <pre key={key}>
        <strong style={{ color: "white", background: "red" }}>{key} 💩</strong>
        {JSON.stringify(val, "", " ")}
      </pre>
    ))}
  </div>
);

render(<Dump props={"hey"} />);
```

</MainContainer>
